{
  "name": "tidb-gateway",
  "version": "1.0.0",
  "scripts": {
    "start": ""npm install --omit=dev && node index.js""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0"
  }
}
